<% if @social_media_link.errors.any? %>
  
  $('div.errors').show();

<% else %>

$('div.errors').hide();
$('div.success').show();
setTimeout(
  function() 
  {
    //do something special
    $('#myModal-content').foundation('reveal', 'close');
  }, 1000);

//refresh partial
$('div#your_social_media_links')
	.html("<%= escape_javascript render(partial: 
		"pages/dashboard/your_social_media_links") %>"
	);

//
$('.best_in_place').best_in_place();
$(document).foundation();

//
<% end %>