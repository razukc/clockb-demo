<nav class="top-bar" data-topbar data-no-turbolink>
<ul class="title-area">
    <li class="name"></li>
    <li class="toggle-topbar menu-icon"><a href="#">Menu</a></li>
  </ul>

<section class="top-bar-section">
  <!-- Right Nav Section -->
  <ul class="right customgap">
    <% if member_signed_in? %>
    <li><%= link_to 'My Events', my_events_path, class: 'button secondary' %></li>
      <li><%= link_to 'Sign out', destroy_member_session_path, :method => 'delete', class: 'button' %></li>
      <li><%= link_to 'Profile', edit_member_registration_path, class: 'button secondary'%></li>
      <% else %>
      <li><%= link_to 'Sign in', new_member_session_path, class: 'button' %></li>
      <li><%= link_to 'Sign up', new_member_registration_path, class: 'button secondary' %></li>
    <% end %>
    
  </ul>

  <!-- Left Nav Section -->
  <ul class="left">
    <%= render 'layouts/navigation_links' %>
  </ul>
</section>
</nav>