<% if @networking_requirement.errors.any? %>
  
  $('div.errors').show();
 <% @networking_requirement.errors.each do |k,v| %>
 <%= k  %>
 <%= "---" %>
 <%= v %>
 <% end %>

<% else %>

$('div.errors').hide();
$('div.success').show();
setTimeout(
  function() 
  {
    //do something special
    $('#myModal-content').foundation('reveal', 'close');
  }, 1000);

//refresh partial
$('div#your_networking_requirements')
	.html("<%= escape_javascript render(partial: 
		"pages/dashboard/your_networking_requirements") %>"
	);

//
$('.best_in_place').best_in_place();
$(document).foundation();

//
<% end %>