<% if @logo_and_image.errors.any? %>
  
  $('div.errors').show();

<% else %>

$('div.errors').hide();
$('div.success').show();
setTimeout(
  function() 
  {
    //do something special
	$('div.success').hide();
    $('#myModal-content').foundation('reveal', 'close');
  }, 1000);

//refresh partial
$('div#your_logo_and_image')
	.html("<%= escape_javascript render(partial: 
		"pages/dashboard/your_logo_and_image") %>"
	);

//
$('.best_in_place').best_in_place();
$(document).foundation();

//
<% end %>